{{#if focusObject}}
    <form-widget {form-object}="focusObject" show-saving (submit)="saveObject" {fields}="_fields" (cancel)="setPage('list')" />
    {{else}}
    <p>Please click the <i class="fa fa-pencil"></i> Edit button on the list page to edit a row.</p>
{{/if}}
